{
    "name": "file-exercise",
    "config": {
        "connector.class": "com.acme.kafka.connect.exercises.file.SampleFileConnector",
        "monitor.thread.timeout": 1000,
        "tasks.max": "1"
    }
}
