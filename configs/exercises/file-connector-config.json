{
    "name": "file-exercise",
    "config": {
        "connector.class": "com.acme.kafka.connect.solutions.file.SampleFileConnector",
        "monitor.thread.timeout": 1000,
        "tasks.max": "1"
    }
}
