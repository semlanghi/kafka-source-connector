{
    "name": "mysql-solutions",
    "config": {
        "connector.class": "com.acme.kafka.connect.solutions.mysql.SampleMySQLConnector",
        "example.db.hostname": "db",
        "example.db.query": "SELECT * FROM subscription WHERE type='daily' LIMIT 100",
        "example.kafka.topic": "subscriptionT",
        "example.db.port": 3306,
        "example.db.user": "root",
        "example.db.database": "kafka_test",
        "example.db.password": "admin",
        "tasks.max": "1"
    }
}
