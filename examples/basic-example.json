{
    "name": "basic-example-1",
    "config": {
        "connector.class": "com.acme.kafka.connect.sample.SampleMySQLConnector",
        "example.db.hostname": "db",
        "example.db.port": 3306,
        "example.db.user": "root",
        "example.db.database": "kafka_test",
        "example.db.password": "admin",
        "tasks.max": "1"
    }
}
